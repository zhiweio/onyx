# Default values for onyx.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
#
# Companion overlays in this directory:
#   - values-lite.yaml      Minimal deploy (no vector DB / Redis / model servers).
#   - values-localdev.yaml  Laptop-sized overlay for kind/minikube; see
#                           /docs/craft/dev/local-kubernetes.md for the full workflow.

global:
  # Global version for all Onyx components (overrides .Chart.AppVersion)
  # Every Onyx image publishes a -dev twin for each of its tags (e.g. "latest-dev",
  # "v1.2.3-dev"), so this single value can select the dev variant of the whole
  # deployment. Today only the backend image actually differs: its -dev twin adds
  # debugging tools (vim, nano, curl, ps, psql) that the default image leaves out.
  version: "latest"
  # Global pull policy for all Onyx component images
  pullPolicy: "IfNotPresent"
  # Host for all Onyx components
  host: "0.0.0.0"

# -- Custom (private/internal) CA certificates for outbound TLS.
# When enabled, every pod that makes outbound TLS connections mounts the
# referenced Secret/ConfigMap and trusts its roots. Keys must be PEM certs
# ending in `.crt` (model servers also accept `.pem`). The handling varies by
# pod type:
#   - Shell-based pods (api server, celery workers, bots, MCP server) mount at
#     /usr/local/share/ca-certificates, run `update-ca-certificates` at startup,
#     and get REQUESTS_CA_BUNDLE / SSL_CERT_FILE pointed at the merged system
#     bundle. These require the container to run as root (the chart default);
#     incompatible with runAsNonRoot overrides.
#   - Model servers run on a distroless image with no shell, so they mount at
#     /etc/onyx/certs and merge the mounted roots with certifi's public roots in
#     Python at startup (additive — public roots stay trusted). No root required.
#   - The Craft sandbox proxy runs non-root on a read-only root filesystem. An
#     init container builds an additive bundle in its writable confdir and
#     configures mitmproxy through SANDBOX_PROXY_SSL_VERIFY_UPSTREAM_TRUSTED_CA
#     to use that bundle for upstream TLS verification.
# Version coupling: chart >= 0.7.0 pairs with the distroless model-server image
# and can't run older ones — the model servers now run nonroot (1001, see their
# securityContext below) and read ONYX_CUSTOM_CA_CERTS_DIR. Upgrade the chart and
# the model-server image together: an old chart on the new image crash-loops (no
# shell for update-ca-certificates), and this chart on an old, root-built image
# can't write the image's root-owned dirs as 1001.
customCACerts:
  enabled: false
  # Exactly one of secretName / configMapName must be set when enabled.
  secretName: ""
  configMapName: ""

# -- TLS verification for PostgreSQL connections.
# When enabled, the chart mounts one CA certificate into each backend workload
# that connects to PostgreSQL and configures libpq-compatible POSTGRES_SSL*
# settings. The certificate source must already exist in the release namespace.
postgresTls:
  enabled: false
  # verify-ca validates the server certificate without requiring POSTGRES_HOST
  # to be one of the certificate's DNS names. Use verify-full when it is.
  sslMode: verify-ca
  # Exactly one of caSecretName / caConfigMapName must be set when enabled.
  caSecretName: ""
  caConfigMapName: ""
  caKey: ca.crt
  # Full path exposed to POSTGRES_SSLROOTCERT.
  caMountPath: /etc/postgres-ca/ca.crt

# -- Verified TLS connections to Redis.
# When enabled, the chart mounts one Redis server CA certificate into each
# backend workload that uses Redis and configures REDIS_SSL with certificate
# verification. Use an external Redis service (`redis.enabled: false`), because
# the bundled Redis does not support TLS. The certificate source must already
# exist in the release namespace.
redisTls:
  enabled: false
  # Verify that the Redis certificate matches REDIS_HOST. Disable only when
  # the endpoint cannot present a certificate for its configured host or IP.
  checkHostname: true
  # Exactly one of caSecretName / caConfigMapName must be set when enabled.
  caSecretName: ""
  caConfigMapName: ""
  caKey: ca.crt
  # Full path exposed to REDIS_SSL_CA_CERTS. The parent directory must exist
  # in every Redis client image because Kubernetes mounts this as one file.
  caMountPath: /etc/ssl/certs/redis-ca.crt

# Uncomment to skip the bundled CNPG operator and its CRDs, and let an existing
# cluster-managed CloudNativePG operator reconcile the Cluster CR below (keep
# postgresql.enabled: true). Two operators/CRD copies on one cluster drift in
# version and fight over ownership of the cluster-scoped postgresql.cnpg.io
# CRDs and webhook configurations — see
# https://github.com/onyx-dot-app/onyx/issues/12490. Unset = follow
# postgresql.enabled. Mirrors redisOperator below.
# postgresqlOperator:
#   enabled: false

postgresql:
  enabled: true
  # IMPORTANT: This nameOverride is required for the CloudNativePG operator to find itself.
  # The operator looks for a deployment with label app.kubernetes.io/name=cloudnative-pg,
  # but since the subchart is aliased as "postgresql", Helm defaults to that name.
  nameOverride: cloudnative-pg
  # Uncomment to override, independently of postgresqlOperator.enabled below,
  # whether the bundled CNPG CRDs (onyx-cnpg-crds subchart) are installed.
  # Unset = follow postgresqlOperator.enabled, then postgresql.enabled.
  # installCRDs: false

  # Disable the CNPG subchart's own templated CRD copy — the onyx-cnpg-crds
  # subchart is the single source of the CRDs (its crds/ dir gets Helm's
  # pre-install treatment, which templated CRDs don't).
  crds:
    create: false
  cluster:
    instances: 1
    # Use a CNPG image that ships pgvector when this chart creates the cluster.
    # imageName: ghcr.io/tensorchord/cloudnative-pgvecto.rs:16.6-v0.3.1
    storage:
      storageClass: ""
      size: 10Gi
    enableSuperuserAccess: true
    superuserSecret:
      name: onyx-postgresql  # keep in sync with auth.postgresql

# -- Master toggle for vector database support. When false:
#   - Sets DISABLE_VECTOR_DB=true on all backend pods
#   - Skips the indexing model server deployment (embeddings not needed)
#   - Skips ALL celery worker deployments (beat, primary, light, heavy,
#     monitoring, user-file-processing, docprocessing, docfetching) — the
#     API server handles background work via FastAPI BackgroundTasks
#   - You should also set opensearch.enabled=false to prevent that subchart from
#     deploying
vectorDB:
  enabled: true

opensearch:
  # Enabled by default. Override to false and set the appropriate env vars in
  # the instance-specific values yaml if using AWS-managed OpenSearch, or simply
  # override to false to entirely disable.
  enabled: true
  # These values are passed to the opensearch subchart.
  # See https://github.com/opensearch-project/helm-charts/blob/main/charts/opensearch/values.yaml

  singleNode: true  # Forces replicas=1, sets discovery.type=single-node

  # Determines service DNS: onyx-opensearch-master.<namespace>.svc.cluster.local
  clusterName: "onyx-opensearch"
  nodeGroup: "master"
  masterService: "onyx-opensearch-master"

  replicas: 1

  image:
    repository: "opensearchproject/opensearch"
    tag: ""  # Empty uses chart's appVersion (3.4.0).

  # The security plugin requires OPENSEARCH_INITIAL_ADMIN_PASSWORD for
  # OpenSearch 2.12+.
  # See https://docs.opensearch.org/latest/install-and-configure/install-opensearch/helm/#prerequisites
  extraEnvs:
    - name: OPENSEARCH_INITIAL_ADMIN_PASSWORD
      valueFrom:
        secretKeyRef:
          name: onyx-opensearch  # Must match auth.opensearch.secretName or auth.opensearch.existingSecret if defined.
          key: opensearch_admin_password  # Must match auth.opensearch.secretKeys value.

  resources:
    requests:
      cpu: 2000m
      memory: 4Gi
    limits:
      cpu: 4000m
      memory: 8Gi

  persistence:
    enabled: true
    # A moderately sized index fills 30Gi; hitting the disk flood-stage
    # watermark write-blocks the whole index. Existing PVCs are not resized by
    # upgrades — expand them online instead.
    size: 64Gi
    storageClass: ""

  # Java heap should be ~50% of memory limit.
  # See https://opster.com/guides/opensearch/opensearch-basics/opensearch-heap-size-usage-and-jvm-garbage-collection/
  # Xms is the starting size, Xmx is the maximum size. These should be the same.
  opensearchJavaOpts: "-Xmx4g -Xms4g"

persistent:
  storageClassName: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

autoscaling:
  # Valid options: 'hpa' (default) or 'keda'.
  # Set to 'keda' to render KEDA ScaledObjects for components that have autoscaling enabled.
  # When using KEDA you must install and manage the KEDA operator separately; it is not bundled with this chart.
  engine: hpa

inferenceCapability:
  extraEnv: []
  volumes: []
  volumeMounts: []
  service:
    portName: modelserver
    type: ClusterIP
    servicePort: 9000
    targetPort: 9000
  name: inference-model-server
  replicaCount: 1
  labels:
    - key: app
      value: inference-model-server
  image:
    repository: onyxdotapp/onyx-model-server
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  containerPorts:
    server: 9000
  podLabels:
    - key: app
      value: inference-model-server
  # Embedding work is bursty: a modest CPU request keeps the pod schedulable
  # on smaller nodes while the limit provides burst headroom for query-time
  # embedding.
  resources:
    requests:
      cpu: 1000m
      memory: 3Gi
    limits:
      cpu: 4000m
      memory: 10Gi
  # Optional health probes
  # Example:
  # readinessProbe:
  #   httpGet:
  #     path: /health
  #     port: model-server
  startupProbe: {}
  readinessProbe: {}
  livenessProbe: {}
  # Runs as the image's onyx user (UID 1001), which owns the venv, HF cache, and
  # /var/log/onyx. customCACerts no longer needs root — the distroless server
  # merges custom roots in Python, not via update-ca-certificates. NOTE: requires
  # the distroless model-server image; an older, root-built image can't write its
  # root-owned dirs as 1001. To run as root, set runAsNonRoot: false and
  # runAsUser: 0 in both this block and podSecurityContext (setting only
  # runAsUser: 0 while runAsNonRoot stays true is rejected at admission).
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1001
    fsGroup: 1001
  securityContext:
    capabilities:
      drop:
        - ALL
    readOnlyRootFilesystem: false
    runAsNonRoot: true
    runAsUser: 1001
    allowPrivilegeEscalation: false
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # Deployment strategy - use Recreate or RollingUpdate with maxSurge: 0 to terminate old pod first
  # This prevents pending pods when cluster resources are constrained
  strategy: {}
  # Example for RollingUpdate that terminates old pod first:
  # strategy:
  #   type: RollingUpdate
  #   rollingUpdate:
  #     maxSurge: 0
  #     maxUnavailable: 1


indexCapability:
  extraEnv: []
  volumes: []
  volumeMounts: []
  service:
    portName: modelserver
    type: ClusterIP
    servicePort: 9000
    targetPort: 9000
  replicaCount: 1
  name: indexing-model-server
  deploymentLabels:
    app: indexing-model-server
  podLabels:
    scope: onyx-backend
  indexingOnly: "True"
  podAnnotations: {}
  containerPorts:
    server: 9000
  image:
    repository: onyxdotapp/onyx-model-server
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  limitConcurrency: 10
  # Indexing embeds in batches and tolerates throttling at low ingest volume;
  # the limit, not the request, governs re-index throughput. Deployments doing
  # sustained large re-indexes should raise the CPU limit (and consider a
  # second replica) — an embedder pegged at its limit for hours can starve
  # docprocessing heartbeats and trip the indexing watchdog.
  resources:
    requests:
      cpu: 1000m
      memory: 3Gi
    limits:
      cpu: 6000m
      memory: 6Gi
  # Optional health probes
  # Example:
  # readinessProbe:
  #   httpGet:
  #     path: /health
  #     port: model-server
  startupProbe: {}
  readinessProbe: {}
  livenessProbe: {}
  # Runs as the image's onyx user (UID 1001), which owns the venv, HF cache, and
  # /var/log/onyx. customCACerts no longer needs root — the distroless server
  # merges custom roots in Python, not via update-ca-certificates. NOTE: requires
  # the distroless model-server image; an older, root-built image can't write its
  # root-owned dirs as 1001. To run as root, set runAsNonRoot: false and
  # runAsUser: 0 in both this block and podSecurityContext (setting only
  # runAsUser: 0 while runAsNonRoot stays true is rejected at admission).
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1001
    fsGroup: 1001
  securityContext:
    capabilities:
      drop:
        - ALL
    readOnlyRootFilesystem: false
    runAsNonRoot: true
    runAsUser: 1001
    allowPrivilegeEscalation: false
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # Deployment strategy - use Recreate or RollingUpdate with maxSurge: 0 to terminate old pod first
  # This prevents pending pods when cluster resources are constrained
  strategy: {}
  # Example for RollingUpdate that terminates old pod first:
  # strategy:
  #   type: RollingUpdate
  #   rollingUpdate:
  #     maxSurge: 0
  #     maxUnavailable: 1
config:
  envConfigMapName: env-configmap

tooling:
  pgInto:
    # -- Mounts a small helper script into app pods that opens psql using the pod's POSTGRES_* env vars.
    # NOTE: the default backend images do not ship psql; set a -dev suffixed global.version
    # (e.g. v1.2.3-dev) or use an image that provides the client binary.
    enabled: false
    # -- Where to place the helper inside the container.
    mountPath: /usr/local/bin/pginto
    # -- Which client binary to call; change if your image uses a non-default path.
    psqlBinary: psql

monitoring:
  grafana:
    dashboards:
      # -- Set to true to deploy Grafana dashboard ConfigMaps for the Onyx indexing pipeline.
      # Requires kube-prometheus-stack (or equivalent) with the Grafana sidecar enabled and watching this namespace.
      # The sidecar must be configured with label selector: grafana_dashboard=1
      enabled: false
  serviceMonitors:
    # -- Set to true to deploy ServiceMonitor resources for API server and Celery worker metrics endpoints.
    # Requires the Prometheus Operator CRDs (included in kube-prometheus-stack).
    # Use `labels` to match your Prometheus CR's serviceMonitorSelector (e.g. release: onyx-monitoring).
    enabled: false
    labels: {}

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

nginx:
  enabled: true
  # Nginx proxy timeout settings (in seconds)
  # These apply to both the internal nginx proxy AND the ingress-nginx annotations.
  # Must be high enough to cover deep research (30+ min sessions) and LLM TTFT
  # for large prompts (70K+ tokens). 900s matches the craft/build timeout in production.
  timeouts:
    connect: 300  # Time to establish connection with upstream server
    send: 900     # Time to send request to upstream server
    read: 900     # Time to read response from upstream server
  controller:
    containerPort:
      http: 1024

    # NOTE: When onyx-nginx-conf changes, nginx pods need to restart.
    # The ingress-nginx subchart doesn't auto-detect our custom ConfigMap changes.
    # Workaround: Helm upgrade will restart if the following annotation value changes.
    podAnnotations:
      onyx.app/nginx-config-version: "7"

    # Propagate DOMAIN into nginx so server_name continues to use the same env var
    extraEnvs:
      - name: DOMAIN
        value: localhost

    config:
      # Hide the nginx version in Server headers/error pages. Set via the
      # controller's supported knob — do NOT put `server_tokens` in the
      # http-snippet files: ingress-nginx already emits the directive in the
      # http{} block, and a duplicate fails `nginx -t` (config reload/startup).
      server-tokens: "false"
      # Expose DOMAIN to the nginx config and pull in our custom snippets
      main-snippet: |
        env DOMAIN;
      http-snippet: |
        include /etc/nginx/custom-snippets/upstreams.conf;
        include /etc/nginx/custom-snippets/server.conf;

    # Mount the existing nginx ConfigMap that holds the upstream and server snippets
    extraVolumes:
      - name: nginx-config
        configMap:
          name: onyx-nginx-conf
    extraVolumeMounts:
      - name: nginx-config
        mountPath: /etc/nginx/custom-snippets
        readOnly: true

    service:
      type: LoadBalancer
      ports:
        http: 80
      targetPorts:
        http: http

webserver:
  extraEnv: []
  replicaCount: 1
  image:
    repository: onyxdotapp/onyx-web-server
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  deploymentLabels:
    app: web-server
  podAnnotations: {}
  podLabels:
    scope: onyx-frontend
  podSecurityContext:
    {}
    # fsGroup: 2000

  securityContext:
    {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000

  containerPorts:
    server: 3000

  service:
    type: ClusterIP
    servicePort: 3000
    targetPort: http

  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi

  # Optional health probes
  # Example:
  # readinessProbe:
  #   httpGet:
  #     path: /api/health
  #     port: http
  startupProbe: {}
  readinessProbe: {}
  livenessProbe: {}

  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    # Example: customTriggers: []
    #   - type: prometheus
    #     metadata:
    #       serverAddress: http://prometheus:9090
    #       metricName: http_requests_per_second
    #       threshold: '100'
    customTriggers: []

  # Additional volumes on the output Deployment definition.
  volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

  # Additional volumeMounts on the output Deployment definition.
  volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

  nodeSelector: {}
  tolerations: []
  affinity: {}

api:
  extraEnv: []
  replicaCount: 1
  image:
    repository: onyxdotapp/onyx-backend
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  deploymentLabels:
    app: api-server
  podAnnotations: {}
  podLabels:
    scope: onyx-backend

  containerPorts:
    server: 8080

  # Uvicorn worker processes per pod. Each worker is its own process with its own
  # event loop and threadpool, so >1 adds parallel capacity and keeps a wedged
  # worker from taking down the pod's /health. The app lifespan runs per worker
  # (same as adding replicas, which already run it concurrently), so this is
  # safe; size against pod CPU and per-worker memory. Default 1 = prior behavior.
  workers: 1

  # Max concurrent connections per uvicorn worker. When set, the worker rejects
  # new connections past this (503) instead of queueing into a thread-starvation
  # wedge. Empty = no cap (uvicorn default). Example: 200.
  limitConcurrency: ""

  # Size of the anyio threadpool serving sync endpoints, including the streaming
  # chat generator (each long-running request — deep research, code interpreter —
  # holds one thread for its whole duration). 0 keeps the anyio default (40).
  # Raise for workloads with many concurrent long agent requests; each thread is
  # real, so size against CPU/memory.
  threadpoolSize: 0

  podSecurityContext:
    {}
    # fsGroup: 2000

  securityContext:
    {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000

  service:
    type: ClusterIP
    servicePort: 8080
    targetPort: api-server-port
    portName: api-server-port

  # CPU limit must stay well above the request: under concurrent slow-stream
  # load a 1-core limit exhausts the CFS quota and stalls the async /health
  # endpoint, causing liveness kills. Idle RSS is ~1.1Gi, so keep the memory
  # request at or above 1Gi.
  resources:
    requests:
      cpu: 1000m
      memory: 1Gi
    limits:
      cpu: 3000m
      memory: 3Gi

  # Optional health probes.
  #
  # Match the path to the probe. /health/ready reports not-ready once the request
  # threadpool is saturated, which is what should pull a pod out of the Service.
  # /health only reports that the process is up. Do NOT put /health/ready on the
  # liveness probe: saturation is load-induced and hits every replica at once,
  # so restarting on it turns a slowdown into an outage.
  #
  # Example:
  # readinessProbe:
  #   httpGet:
  #     path: /health/ready
  #     port: api-server-port
  # livenessProbe:
  #   httpGet:
  #     path: /health
  #     port: api-server-port
  startupProbe: {}
  readinessProbe: {}
  livenessProbe: {}

  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    # Example: customTriggers: []
    #   - type: prometheus
    #     metadata:
    #       serverAddress: http://prometheus:9090
    #       metricName: http_requests_per_second
    #       threshold: '100'
    customTriggers: []

  # Additional volumes on the output Deployment definition.
  volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

  # Additional volumeMounts on the output Deployment definition.
  volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

  nodeSelector: {}
  tolerations: []
  affinity: {}

  # Disable only when a separate deployment step runs migrations.
  runStartupMigrations: true
  # Run update-ca-certificates before starting the server.
  # DEPRECATED: prefer the top-level customCACerts block, which covers all
  # outbound-TLS pods. This flag only affects the API server; useful when
  # mounting custom CA certificates manually via api.volumes/api.volumeMounts.
  # NOTE: Requires the container to run as root (runAsUser: 0).
  # CA certificate files must be mounted under /usr/local/share/ca-certificates/
  # with a .crt extension (e.g. /usr/local/share/ca-certificates/my-ca.crt).
  # NOTE: Python HTTP clients (requests, httpx) use certifi's bundle by default
  # and will not pick up the system CA store automatically. Set the following
  # environment variables via configMap values (loaded through envFrom) to make them use the updated system bundle:
  #   REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
  #   SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
  runUpdateCaCertificates: false


######################################################################
#
# Background workers
#
######################################################################

celery_shared:
  image:
    repository: onyxdotapp/onyx-backend
    tag: ""  # Overrides the image tag whose default is the chart appVersion.
  startupProbe:
    # startupProbe fails after 2m
    exec:
      command: ["test", "-f", "/app/onyx/main.py"]
    failureThreshold: 24
    periodSeconds: 5
    timeoutSeconds: 3
  readinessProbe:
    # readinessProbe fails after 15s + 2m of inactivity
    # it's ok to see the readinessProbe fail transiently while the container starts
    initialDelaySeconds: 15
    periodSeconds: 5
    failureThreshold: 24
    timeoutSeconds: 3
  livenessProbe:
    # livenessProbe fails after 5m of inactivity
    initialDelaySeconds: 60
    periodSeconds: 60
    failureThreshold: 5
    timeoutSeconds: 3
  podSecurityContext: {}
  securityContext:
    # `privileged` is not needed by this workload and is intentionally omitted.
    # root is kept only so the optional customCACerts feature can run
    # update-ca-certificates; to run non-root, override this and
    # podSecurityContext (see codeInterpreter below for the pattern).
    runAsUser: 0

celery_beat:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  # Path of beat's schedule file. Kept under /tmp — where the chart mounts an
  # emptyDir (unless volumeMounts below already provide /tmp) — so beat runs
  # under hardened securityContexts (non-root or readOnlyRootFilesystem),
  # which can't write celery's default location, the working directory /app.
  # Set to "" to restore celery's default.
  schedulePath: "/tmp/celerybeat-schedule"
  # Extra CLI args appended to the celery beat command, after the flags above.
  extraArgs: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-beat
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_heavy:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-heavy
  # Pruning and permission-sync walk large connector corpora; 2Gi limits have
  # OOMed in production on big syncs.
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 1000m
      memory: 4Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_docprocessing:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 20
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-docprocessing
  resources:
    requests:
      cpu: 500m
      memory: 2Gi
    limits:
      cpu: 1000m
      memory: 12Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_light:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-light
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 2000m
      memory: 4Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_monitoring:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-monitoring
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 4Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_primary:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-primary
  resources:
    requests:
      cpu: 500m
      memory: 2Gi
    limits:
      cpu: 1000m
      memory: 4Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_user_file_processing:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-user-file-processing
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 2000m
      memory: 2Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Dedicated worker for Craft scheduled-task headless executor fires.
# Isolated from `heavy` because each fire is long-running (LLM + tool calls
# in a sandbox) and would otherwise starve pruning / perms-sync / csv-export
# of worker slots. The dispatcher and stuck-run sweeper are lightweight
# DB-only coordination tasks and run on the primary queue instead.
celery_worker_scheduled_tasks:
  extraEnv: []
  replicaCount: 1
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-scheduled-tasks
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 2000m
      memory: 2Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Discord bot for Onyx
# The bot offloads message processing to scalable API pods via HTTP requests.
discordbot:
  extraEnv: []
  enabled: false  # Disabled by default - requires bot token configuration
  # Bot token can be provided directly or via a Kubernetes secret
  # Option 1: Direct token (not recommended for production)
  botToken: ""
  # Option 2: Reference a Kubernetes secret (recommended)
  botTokenSecretName: ""  # Name of the secret containing the bot token
  botTokenSecretKey: "token"  # Key within the secret (default: "token")
  # Command prefix for bot commands (default: "!")
  invokeChar: "!"
  image:
    repository: onyxdotapp/onyx-backend
    tag: ""  # Overrides the image tag whose default is the chart appVersion.
  podAnnotations: {}
  podLabels:
    scope: onyx-backend
  deploymentLabels:
    app: discord-bot
  podSecurityContext:
    {}
  securityContext:
    {}
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "1000m"
      memory: "2000Mi"
  volumes: []
  volumeMounts: []
  nodeSelector: {}
  tolerations: []
  affinity: {}

slackbot:
  extraEnv: []
  enabled: true
  replicaCount: 1
  image:
    repository: onyxdotapp/onyx-backend
    tag: ""  # Overrides the image tag whose default is the chart appVersion.
  podAnnotations: {}
  podLabels:
    scope: onyx-backend
  deploymentLabels:
    app: slack-bot
  podSecurityContext:
    {}
  securityContext:
    {}
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "1000m"
      memory: "2000Mi"
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Onyx Model Context Protocol (MCP) Server
# Allows LLMs to use Onyx like invoking tools or accessing resources
mcpServer:
  extraEnv: []
  enabled: false  # Disabled by default
  replicaCount: 1
  image:
    repository: onyxdotapp/onyx-backend
    tag: ""  # Overrides the image tag whose default is the chart appVersion.
  # CORS origins for MCP clients (comma-separated)
  # Example: "https://claude.ai,https://app.cursor.sh"
  corsOrigins: ""
  podAnnotations: {}
  podLabels:
    scope: onyx-backend
  deploymentLabels:
    app: mcp-server
  containerPorts:
    server: 8090
  service:
    type: ClusterIP
    servicePort: 8090
    targetPort: mcp-server-port
    portName: mcp-server-port
  podSecurityContext: {}
  securityContext: {}
  resources:
    requests:
      cpu: "250m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  livenessProbe:
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
  readinessProbe:
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  volumes: []
  volumeMounts: []
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Commercial MCP gateway (Patsnap / Qixinbao / Tianyancha and generic HTTP)
mcpGateway:
  extraEnv: []
  enabled: false
  replicaCount: 1
  image:
    repository: onyxdotapp/onyx-backend
    tag: ""
  podAnnotations: {}
  podLabels:
    scope: onyx-backend
  deploymentLabels:
    app: mcp-gateway
  containerPorts:
    server: 8091
  service:
    type: ClusterIP
    servicePort: 8091
    targetPort: mcp-gateway-port
    portName: mcp-gateway-port
  podSecurityContext: {}
  securityContext: {}
  resources:
    requests:
      cpu: "250m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  livenessProbe:
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
  readinessProbe:
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  volumes: []
  volumeMounts: []
  nodeSelector: {}
  tolerations: []
  affinity: {}

celery_worker_docfetching:
  extraEnv: []
  replicaCount: 1
  # Grace for the watchdog to mark its in-flight attempt INTERRUPTED and stop its
  # subprocess on SIGTERM. Must exceed the watchdog poll interval + subprocess grace.
  terminationGracePeriodSeconds: 60
  # Per-worker log level override. Empty (default) means use the global
  # LOG_LEVEL env var. Set to INFO/DEBUG/WARNING/etc. to override LOG_LEVEL for
  # this worker only — this becomes `--loglevel=<value>` on the celery worker
  # command and takes precedence over LOG_LEVEL inside the worker process.
  logLevel: ""
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 20
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
    # KEDA specific configurations (only used when autoscaling.engine is set to 'keda')
    pollingInterval: 30  # seconds
    cooldownPeriod: 300  # seconds
    failureThreshold: 3  # number of failures before fallback
    fallbackReplicas: 1  # replicas to maintain on failure
    # Custom triggers for advanced KEDA configurations
    customTriggers: []
  podAnnotations: {}
  podLabels:
    scope: onyx-backend-celery
  deploymentLabels:
    app: celery-worker-docfetching
  resources:
    requests:
      cpu: 500m
      memory: 2Gi
    limits:
      cpu: 1000m
      memory: 16Gi
  volumes: []  # Additional volumes on the output Deployment definition.
  volumeMounts: []  # Additional volumeMounts on the output Deployment definition.
  nodeSelector: {}
  tolerations: []
  affinity: {}

######################################################################
#
# End background workers section
#
######################################################################

# Uncomment to skip the bundled redis-operator and let an existing cluster-wide
# operator reconcile the Redis CR below (keep redis.enabled: true). Your operator
# must provide the redis.redis.opstreelabs.in/v1beta2 CRDs. Unset = follow redis.enabled.
# redisOperator:
#   enabled: false

redis:
  enabled: true
  redisStandalone:
    image: quay.io/opstree/redis
    tag: v7.0.15
    imagePullPolicy: IfNotPresent
    serviceType: ClusterIP
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi
    # Use existing secret for Redis password
    redisSecret:
      secretName: onyx-redis
      secretKey: redis_password
  # Redis configuration
  externalConfig:
    enabled: true
    data: |
      appendonly no
      save ""
      maxmemory 400mb
      maxmemory-policy allkeys-lru
      timeout 0
      tcp-keepalive 300
  storageSpec:
    volumeClaimTemplate:
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi

minio:
  enabled: true
  mode: standalone
  replicas: 1
  drivesPerNode: 1
  existingSecret: onyx-objectstorage
  buckets:
    - name: onyx-file-store-bucket
  persistence:
    enabled: true
    size: 30Gi
    storageClass: ""
  service:
    type: ClusterIP
    port: 9000
  consoleService:
    type: ClusterIP
    port: 9001
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Code Interpreter - Python code execution service (beta feature)
# The code-interpreter subchart's enable condition lives at
# codeInterpreter.enabled, but the subchart itself is configured under the
# hyphenated `code-interpreter:` key below — the dependency has no alias, so
# Helm only passes values nested under the dependency's name. Any other key
# placed in THIS block is silently ignored.
codeInterpreter:
  enabled: true

  # Used by the parent chart to compose CODE_INTERPRETER_BASE_URL; must match
  # the subchart's service port.
  service:
    port: 8000

# Values passed through to the code-interpreter subchart.
code-interpreter:
  # API container resources. Workspace archives staged into sandboxes are
  # routinely tens of MB and the base64-encoded snapshot path roughly doubles
  # in-flight memory; the subchart's default 512Mi limit repeatedly OOMKilled
  # this pod in production (and parent OOMs leak spawned code-exec pods).
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 2Gi

ingress:
  enabled: false
  className: ""
  api:
    host: onyx.local
  webserver:
    host: onyx.local

letsencrypt:
  enabled: false
  email: "abc@abc.com"

# -- Sync secret material from an external secret manager (AWS Secrets Manager,
# GCP Secret Manager, Vault, ...) via External Secrets Operator. When enabled,
# the chart renders a single ExternalSecret CR that materializes one Kubernetes
# Secret containing all keys from the upstream blob. Point each auth.<X>.existingSecret
# at externalSecret.secretName so the chart's per-section secretKeys mappings
# read from the materialized Secret. Loose env-var secrets (those that today
# live in configMap blocks) can be projected into pods via extraEnvFromSecret.
#
# PREREQUISITE: External Secrets Operator and a matching SecretStore /
# ClusterSecretStore must exist in the cluster before enabling this. The chart
# does NOT install ESO.
externalSecret:
  enabled: false
  # -- Path/ID of the secret in the upstream provider (e.g. AWS Secrets Manager
  # secret name "onyx/<customer>/app-secrets"). Required when enabled=true.
  refPath: ""
  # -- Name of the materialized Kubernetes Secret. Point auth.<X>.existingSecret
  # and extraEnvFromSecret at this name.
  secretName: onyx-app-secrets
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  refreshInterval: 1h

# -- If set, adds `envFrom: - secretRef: name: <value>` to every backend
# deployment so all keys in the named Secret are injected as env vars. Intended
# for secrets that today live in plaintext under the `configMap:` block and
# should move into a Secret (e.g. LINEAR_CLIENT_SECRET,
# SALESFORCE_CLIENT_ID, SALESFORCE_CLIENT_SECRET, BRAINTRUST_API_KEY).
# Typically set to the same value as externalSecret.secretName.
extraEnvFromSecret: ""

# -- Safeguards against destructive upgrades from chart 0.4.x. Chart 0.5.0
# removed the bundled Vespa subchart; the check below fails `helm upgrade`
# if the legacy `da-vespa` StatefulSet is still in the namespace, so
# the StatefulSet and its Vespa data volume are not silently deleted.
# Set `acknowledged: true` once you've migrated to external Vespa, or
# `enabled: false` to disable the check entirely.
# See deployment/helm/MIGRATION.md.
legacyVespaCheck:
  enabled: true
  acknowledged: false

# -- Governs all Secrets created or used by this chart. Values set by this chart will be base64 encoded in the k8s cluster.
auth:
  postgresql:
    # -- Enable or disable this secret entirely. Will remove from env var configurations and remove any created secrets.
    enabled: true
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-postgresql'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map, key is always upper-cased as an env var
    secretKeys:
      # CloudNativePG requires `username` and `password` keys for the superuser secret.
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
    # -- Secrets values IF existingSecret is empty. Key here must match the value in secretKeys to be used. Values will be base64 encoded in the k8s cluster.
    values:
      username: "postgres"
      password: "postgres"
  redis:
    # -- Enable or disable this secret entirely. Will remove from env var configurations and remove any created secrets.
    enabled: true
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-redis'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map, key is always upper-cased as an env var
    secretKeys:
      REDIS_PASSWORD: redis_password
    # -- Secrets values IF existingSecret is empty. Key here must match the value in secretKeys to be used. Values will be base64 encoded in the k8s cluster.
    values:
      redis_password: "password"
  objectstorage:
    # -- Enable or disable this secret entirely. Will remove from env var configurations and remove any created secrets.
    enabled: true
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-objectstorage'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map, key is always upper-cased as an env var
    secretKeys:
      S3_AWS_ACCESS_KEY_ID: s3_aws_access_key_id
      S3_AWS_SECRET_ACCESS_KEY: s3_aws_secret_access_key
    # -- Secrets values IF existingSecret is empty. Key here must match the value in secretKeys to be used. Values will be base64 encoded in the k8s cluster.
    # SECURITY: no defaults shipped — empty values fail closed (see
    # auth-secrets.yaml) so prod can't run on minioadmin. Set these, or better,
    # use existingSecret/externalSecret above. Keep s3_aws_* == rootUser/rootPassword.
    values:
      s3_aws_access_key_id: ""
      s3_aws_secret_access_key: ""
      rootUser: ""
      rootPassword: ""
  oauth:
    # Legacy pre-v4.4 SSO credentials. Imported into an SSO provider entry on
    # upgrade. Planned for removal in v4.5.
    # -- Enable or disable this secret entirely. Will remove from env var configurations and remove any created secrets.
    enabled: false
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-oauth'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map, key is always upper-cased as an env var
    secretKeys:
      OAUTH_CLIENT_ID: "oauth_client_id"
      OAUTH_CLIENT_SECRET: "oauth_client_secret"
    # -- Secrets values IF existingSecret is empty. Key here must match the value in secretKeys to be used. Values will be base64 encoded in the k8s cluster.
    values:
      oauth_client_id: ""
      oauth_client_secret: ""
  smtp:
    # -- Enable or disable this secret entirely. Will remove from env var configurations and remove any created secrets.
    enabled: false
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-smtp'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map, key is always upper-cased as an env var
    secretKeys:
      SMTP_PASS: "smtp_pass"
    # -- Secrets values IF existingSecret is empty. Key here must match the value in secretKeys to be used. Values will be base64 encoded in the k8s cluster.
    values:
      smtp_pass: ""
  dbreadonly:
    # -- Enable or disable this secret entirely. Will remove from env var configurations and remove any created secrets.
    enabled: false
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-dbreadonly'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map, key is always upper-cased as an env var
    secretKeys:
      DB_READONLY_USER: db_readonly_user
      DB_READONLY_PASSWORD: db_readonly_password
    # -- Secrets values IF existingSecret is empty. Key here must match the value in secretKeys to be used. Values will be base64 encoded in the k8s cluster.
    values:
      db_readonly_user: ""
      db_readonly_password: ""
  opensearch:
    # Enable or disable this secret entirely. Will remove from env var
    # configurations and remove any created secrets.
    # Enabled by default. Override to false and set the appropriate env vars in
    # the instance-specific values yaml if using AWS-managed OpenSearch, or
    # simply override to false to entirely disable.
    enabled: true
    # Overwrite the default secret name, ignored if existingSecret is defined.
    secretName: 'onyx-opensearch'
    # Use a secret specified elsewhere.
    existingSecret: ""
    # This defines the env var to secret map, key is always upper-cased as an
    # env var.
    secretKeys:
      OPENSEARCH_ADMIN_USERNAME: opensearch_admin_username
      OPENSEARCH_ADMIN_PASSWORD: opensearch_admin_password
    # Secrets values IF existingSecret is empty. Key here must match the value
    # in secretKeys to be used. Values will be base64 encoded in the k8s
    # cluster.
    # For the bundled OpenSearch chart, the admin password is consumed during
    # initial cluster setup. Changing this value later will update Onyx's
    # client credentials, but will not rotate the OpenSearch admin password.
    # Set this before first install or use existingSecret to preserve the
    # current secret on upgrade.
    # Password must meet OpenSearch complexity requirements:
    # min 8 chars, uppercase, lowercase, digit, and special character.
    # Required when auth.opensearch.enabled=true and no existing secret exists.
    values:
      opensearch_admin_username: "admin"
      opensearch_admin_password: ""
  userauth:
    # -- Used for password reset / verification tokens and OAuth/OIDC state signing.
    # Disabled by default to preserve upgrade compatibility for existing Helm customers.
    enabled: false
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-userauth'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map
    secretKeys:
      USER_AUTH_SECRET: user_auth_secret
    # -- Secret value. Required when this secret is enabled - generate with: openssl rand -hex 32
    # If not set, helm install/upgrade will fail when auth.userauth.enabled=true.
    values:
      user_auth_secret: ""
  metricsAuth:
    # -- Bearer token guarding the API and MCP servers' /metrics endpoints. /metrics
    # returns 401 by default until you either enable this (provide a token) or
    # explicitly opt out via configMap.DISABLE_METRICS_AUTH="true". When enabled,
    # scrapers must send the token as `Authorization: Bearer <token>` (Prometheus
    # scrape config: `authorization`), and the ServiceMonitors are wired
    # to send it automatically.
    enabled: false
    # -- Only inject the token into pods that serve protected /metrics.
    allPods: false
    # -- Overwrite the default secret name, ignored if existingSecret is defined
    secretName: 'onyx-metrics-auth'
    # -- Use a secret specified elsewhere
    existingSecret: ""
    # -- This defines the env var to secret map
    secretKeys:
      METRICS_AUTH_TOKEN: metrics_auth_token
    # -- Secret value. Required when this secret is enabled - generate with: openssl rand -hex 32
    # If not set, helm install/upgrade will fail when auth.metricsAuth.enabled=true.
    values:
      metrics_auth_token: ""
  # Ed25519 private key for sandbox push auth (only the public key reaches sandbox pods).
  # Generate with: python3 -c "import base64; from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey as K; from cryptography.hazmat.primitives.serialization import *; print(base64.b64encode(K.generate().private_bytes(Encoding.Raw,PrivateFormat.Raw,NoEncryption())).decode())"
  sandboxPushSecret:
    enabled: false
    allPods: false  # Restricted: only injected into pods that provision sandboxes (API server + scheduled-tasks worker)
    secretName: 'onyx-sandbox-push-secret'
    existingSecret: ""
    secretKeys:
      ONYX_SANDBOX_PUSH_PRIVATE_KEY: private_key
    values:
      private_key: ""

# NetworkPolicy for the per-sandbox push daemon (port 8731).
# In prod the strict default is correct: only api_server pods can push.
# Local-dev enables `allowTelepresenceAmbassador` via values-localdev.yaml so
# that traffic intercepted from a developer's laptop (which enters the cluster
# as the telepresence traffic-manager pod) is also permitted.
sandboxPushPolicy:
  allowTelepresenceAmbassador: false

configMap:
  # WARNING: do NOT copy hostname keys from a docker-compose .env into this
  # block. The compose stack uses underscored service names (e.g. `api_server`,
  # `cache`, `inference_model_server`) which are invalid in K8s DNS labels and
  # break inter-pod traffic in ways that look like a hung service.
  # In particular, avoid copying these compose-only hostnames:
  #   REDIS_HOST, INTERNAL_URL, API_SERVER_HOST,
  #   MODEL_SERVER_HOST, INDEXING_MODEL_SERVER_HOST
  # Leave SANDBOX_PROXY_PORT unset; the chart owns the internal proxy port.
  # SANDBOX_PROXY_HOST defaults to the chart-managed Service DNS name and may be
  # overridden only if sandboxes must reach a different proxy host.
  # MODEL_SERVER_HOST / INDEXING_MODEL_SERVER_HOST default to the chart-managed
  # model Services; set them here to point at external model servers instead
  # (the chart defaults are then not rendered).
  # See deployment/helm/README.md for the external-Redis / ElastiCache path.

  # Authentication is always enabled (email/password). SSO (Google/OIDC/SAML)
  # and its per-provider options are configured in the admin panel (Admin
  # Panel > Organization > SSO Providers), not through environment variables.
  # AUTH_TYPE stays as an inert default for the pre-v4.4 SSO import on
  # upgrade. Planned for removal in v4.5.
  # Set auth.userauth.enabled=true and provide auth.userauth.values.user_auth_secret
  # (signs password reset and email verification tokens and OAuth login state).
  AUTH_TYPE: "basic"
  # Opt-in: capture IdP directory claims at OAuth/OIDC login and use them to
  # enrich chat prompts and resolve {{user.<key>}} agent placeholders. Sends
  # directory data to the configured LLM, so off by default.
  IDP_PROFILE_ENRICHMENT_ENABLED: ""
  # Optional JSON overriding the directory claim aliases per profile field, e.g.
  # '{"department": ["dept", "division"], "country": ["c"]}'.
  IDP_PROFILE_CLAIM_MAP: ""
  # 1 Day Default
  SESSION_EXPIRE_TIME_SECONDS: "86400"
  # Can be something like onyx.app, as an extra double-check
  VALID_EMAIL_DOMAINS: ""
  # /metrics requires a bearer token by default and returns 401 until one is set
  # (enable auth.metricsAuth) OR you explicitly opt out here. Set to "true" to
  # expose /metrics with NO authentication.
  DISABLE_METRICS_AUTH: ""
  # The interactive API docs and schema (/openapi.json, /docs, /redoc) are off by
  # default and the routes are not registered (404). Set to "true" to expose them
  # publicly with NO authentication.
  ENABLE_PUBLIC_DOCS: ""
  # For sending verification emails, true or false
  REQUIRE_EMAIL_VERIFICATION: ""
  # If unspecified then defaults to 'smtp.gmail.com'
  SMTP_SERVER: ""
  # For sending verification emails, if unspecified then defaults to '587'
  SMTP_PORT: ""
# 'your-email@company.com'
  SMTP_USER: ""
  # 'your-gmail-password'
  # SMTP_PASS: ""
  # 'your-email@company.com' SMTP_USER missing used instead
  EMAIL_FROM: ""
  # Optional comma-separated BCC archive recipients for all emails
  EMAIL_ARCHIVE_BCC_ADDRESSES: ""
  # MinIO/S3 Configuration override
  S3_ENDPOINT_URL: ""  # only used if minio is not enabled
  S3_FILE_STORE_BUCKET_NAME: ""
  # Azure Blob Storage file store (set FILE_STORE_BACKEND: "azure" and
  # minio.enabled: false). Auth priority: AZURE_STORAGE_CONNECTION_STRING,
  # then AZURE_STORAGE_ACCOUNT_KEY, then DefaultAzureCredential (AKS Workload
  # Identity / managed identity). Put the connection string / account key in a
  # secret rather than here for production.
  AZURE_FILE_STORE_CONTAINER_NAME: ""
  AZURE_FILE_STORE_PREFIX: ""
  AZURE_STORAGE_ACCOUNT_NAME: ""
  # Derived from the account name when unset; set explicitly for sovereign
  # clouds (e.g. *.blob.core.usgovcloudapi.net).
  AZURE_STORAGE_ACCOUNT_URL: ""
  # Gen AI Settings
  GEN_AI_MAX_TOKENS: ""
  LLM_SOCKET_READ_TIMEOUT: "60"
  IMAGE_SUMMARIZATION_TIMEOUT: "300"
  CONTEXTUAL_RAG_LLM_TIMEOUT: "180"
  MAX_CHUNKS_FED_TO_CHAT: ""
  # Usage / cost accounting
  # Records priced generation spans into the per-user usage ledger (default on).
  # Set to "false" to disable the recording processor entirely.
  USER_USAGE_TRACKING_ENABLED: ""
  # Flat per-image cost in cents when litellm has no price for an image model (default 4).
  DEFAULT_IMAGE_COST_CENTS: ""
  # Fallback USD per million tokens when litellm can't price a model (default 0 = free).
  DEFAULT_LLM_INPUT_COST_PER_MTOK: ""
  DEFAULT_LLM_OUTPUT_COST_PER_MTOK: ""
  # Query Options
  DOC_TIME_DECAY: ""
  HYBRID_ALPHA: ""
  # TEMPORARY (self-hosted only, will be removed soon): comma-separated document set
  # NAMES. When set, the Onyx Search UI returns results only from those sets (chat is
  # unaffected). Empty = no restriction.
  FORCED_DOCUMENT_SET_NAMES: ""
  # Don't change the NLP models unless you know what you're doing
  EMBEDDING_BATCH_SIZE: ""
  DOCUMENT_ENCODER_MODEL: ""
  NORMALIZE_EMBEDDINGS: ""
  ASYM_QUERY_PREFIX: ""
  ASYM_PASSAGE_PREFIX: ""
  DISABLE_RERANK_FOR_STREAMING: ""
  MODEL_SERVER_PORT: ""
  MIN_THREADS_ML_MODELS: ""
  # Indexing Configs
  NUM_INDEXING_WORKERS: ""
  # Fail an indexing attempt when its worker exceeds this RSS (MB) instead of
  # letting the pod get OOM-killed; "" = disabled
  INDEXING_WORKER_MEMORY_LIMIT_MB: ""
  INDEXING_WORKER_TRACEMALLOC: ""
  DISABLE_INDEX_UPDATE_ON_SWAP: ""
  # OpenSearch Point-In-Time keep-alive for the reindex/port scan (default 5m)
  PIT_KEEP_ALIVE: ""
  CONTINUE_ON_CONNECTOR_FAILURE: ""
  CONFLUENCE_CONNECTOR_LABELS_TO_SKIP: ""
  JIRA_CLOUD_API_VERSION: ""
  JIRA_SERVER_API_VERSION: ""
  CANVAS_PERMISSION_DOC_SYNC_FREQUENCY: ""
  CANVAS_PERMISSION_GROUP_SYNC_FREQUENCY: ""
  BOX_CONNECTOR_SIZE_THRESHOLD: ""
  BOX_PERMISSION_DOC_SYNC_FREQUENCY: ""
  BOX_PERMISSION_GROUP_SYNC_FREQUENCY: ""
  GONG_CONNECTOR_START_TIME: ""
  NOTION_CONNECTOR_DISABLE_RECURSIVE_PAGE_LOOKUP: ""
  # Push every successfully indexed public-connector document to an external
  # HTTP endpoint (failures never fail indexing). Intended for non-EE
  # deployments — EE users should configure the Document Push hook in the
  # admin UI instead. Not supported in multi-tenant deployments.
  DOCUMENT_PUSH_ENDPOINT_URL: ""
  DOCUMENT_PUSH_API_KEY: ""
  DOCUMENT_PUSH_TIMEOUT_SECONDS: ""
  # Worker Parallelism
  CELERY_WORKER_DOCPROCESSING_CONCURRENCY: ""
  CELERY_WORKER_LIGHT_CONCURRENCY: ""
  CELERY_WORKER_LIGHT_PREFETCH_MULTIPLIER: ""
  CELERY_WORKER_USER_FILE_PROCESSING_CONCURRENCY: ""
  # Per-connector external group sync lock TTL in seconds (default 300); raise
  # toward the 6h crawl deadline if group syncs run long, so periodic
  # re-dispatches can't start concurrent duplicate syncs.
  CELERY_EXTERNAL_GROUP_SYNC_LOCK_TIMEOUT: ""
  # OnyxBot SlackBot Configs
  ONYX_BOT_DISABLE_DOCS_ONLY_ANSWER: ""
  ONYX_BOT_DISPLAY_ERROR_MSGS: ""
  NOTIFY_SLACKBOT_NO_ANSWER: ""
  DISCORD_BOT_TOKEN: ""
  DISCORD_BOT_INVOKE_CHAR: ""
  # Logging
  # Optional Telemetry, please keep it on (nothing sensitive is collected)? <3
  DISABLE_TELEMETRY: ""
  # LOG_LEVEL is the global knob for log verbosity across every Onyx process —
  # api_server, model_server, slack/discord bots, AND all Celery workers. Empty
  # (default) -> Python falls back to "info" for the api/model/etc processes;
  # Celery workers also pick up "" -> info via this same env var. Per-worker
  # overrides: set `.celery_worker_X.logLevel` below to an explicit value
  # (INFO/DEBUG/etc.) — that will render `--loglevel=<value>` into the worker
  # command and take precedence over LOG_LEVEL for that worker only.
  LOG_LEVEL: ""
  LOG_ONYX_MODEL_INTERACTIONS: ""
  # Shared or Non-backend Related
  WEB_DOMAIN: "http://localhost:3000"
  # DOMAIN used by nginx
  DOMAIN: "localhost"
  # Chat Configs
  HARD_DELETE_CHATS: ""
  MAX_ALLOWED_UPLOAD_SIZE_MB: ""
  DEFAULT_USER_FILE_MAX_UPLOAD_SIZE_MB: ""
  # Sandbox config — always "kubernetes" for Helm deployments
  SANDBOX_BACKEND: "kubernetes"
  SANDBOX_NAMESPACE: "onyx-sandboxes"
  SANDBOX_SERVICE_ACCOUNT_NAME: "sandbox"
  # ONYX_SERVER_URL must be the full API base URL, path prefix included — e.g.
  # "https://onyx.example/api" through the public proxy, or
  # "http://<release>-api-service.<ns>.svc.cluster.local:8080" directly at the
  # API service (no prefix). It is required when ENABLE_CRAFT=true.
  SANDBOX_PROXY_CA_SECRET: "sandbox-proxy-ca"
  SANDBOX_PROXY_CA_CONFIGMAP: "sandbox-proxy-ca-bundle"
  # Operator switch for heavier long jobs. Off by default. Raises project
  # file cap, job budgets, specialist cap, and the silent-step inactivity
  # window. Does not change the 30-minute turn hard cap.
  CRAFT_DEEP_JOB_RESOURCES: ""
  CRAFT_DEEP_JOB_PROJECT_MAX_FILES: ""
  CRAFT_DEEP_JOB_TOTAL_BUDGET_SECONDS: ""
  CRAFT_DEEP_JOB_PHASE_BUDGET_SECONDS: ""
  CRAFT_DEEP_JOB_SOFT_BUDGET_FRACTION: ""
  CRAFT_DEEP_JOB_MAX_SPECIALISTS: ""
  CRAFT_PROJECT_MAX_FILES: ""
  CRAFT_PROJECT_MAX_FILE_SIZE_MB: ""
  CRAFT_PROJECT_MAX_TOTAL_SIZE_GB: ""
  SANDBOX_IMAGE_PULL_POLICY: ""
  OPENCODE_LONG_TOOL_INACTIVITY_TIMEOUT_SECONDS: ""
  # MCP Gateway — set mcpGateway.enabled=true to run the process, then turn the
  # module on in Admin Panel > System MCP. Both gates must be open before any
  # system MCP server is visible to users.
  # Lifetime of the signed token the API server sends to the gateway.
  MCP_GATEWAY_TOKEN_TTL_SECONDS: ""
  # Days to keep gateway call-history rows. 0 or empty keeps every row.
  MCP_GATEWAY_CALL_LOG_RETENTION_DAYS: ""
  # Max personal MCP servers one user may create.
  PERSONAL_MCP_MAX_SERVERS: ""
  # MCP result storage. At or below the inline threshold a result stays in
  # Postgres; above it the body goes to the file store. The caller always
  # receives the full cached payload.
  MCP_RESULT_INLINE_THRESHOLD_BYTES: ""
  # Hard ceiling on what the gateway persists; larger responses pass through.
  MCP_RESULT_MAX_BYTES: ""
  MCP_RESULT_DIGEST_MAX_BYTES: ""
  # Stored results untouched for this long are deleted.
  MCP_RESULT_BLOB_TTL_DAYS: ""
# -- Craft sandbox RBAC (rendered when configMap.ENABLE_CRAFT=true).
craft:
  # Extra SAs (release ns) to also grant sandbox RBAC, e.g. a separate worker SA.
  extraBoundServiceAccounts: []
  # CIDR(s) of any non-pod cluster DNS listener the sandbox must reach for firewall-init.sh's
  # proxy-FQDN getent lookup. A NodeLocal DNS cache is a hostNetwork endpoint (its listen IP is
  # cluster-specific — commonly 169.254.20.10, but configurable), so it is not a CoreDNS pod and
  # the kube-dns podSelector in the sandbox-egress NetworkPolicy can't match it; without an allow
  # here its DNS query hits the default-deny and bricks sandbox init. Set this to the actual
  # listener IP(s), e.g. ["169.254.20.10/32"]. Empty (default) = standard CoreDNS, resolved via
  # the podSelector alone (no NodeLocal DNS).
  dnsExtraCIDRs: []
  # CIDR(s) of the egress proxy when it is NOT a chart-labeled pod. The sandbox-egress NetworkPolicy
  # selects the proxy by pod label (app.kubernetes.io/component: sandbox-proxy), but
  # configMap.SANDBOX_PROXY_HOST may be overridden to an external proxy (FQDN/IP) or a Service whose
  # pods don't carry that label. firewall-init.sh allows whatever the host resolves to, but a
  # NetworkPolicy can't follow a hostname, so on NP-enforcing clusters such a proxy is dropped and
  # all sandbox egress breaks. Set this to the proxy CIDR(s), e.g. ["10.0.5.6/32"], on those setups.
  # Empty (default) = the chart-deployed proxy, covered by the podSelector (incl. the
  # SANDBOX_PROXY_NAMESPACE shared-proxy path); leave empty unless you point at a non-pod proxy.
  proxyExtraCIDRs: []

# -- Sandbox Pod scheduling (rendered into the sandbox-pod PodTemplate).
# Image/resources/ports come from the configMap.SANDBOX_* keys.
sandboxPod:
  nodeSelector:
    onyx.app/workload: sandbox
  tolerations:
    - key: workload
      operator: Equal
      value: sandbox
      effect: NoSchedule

# -- Pre-pull the sandbox image onto the sandbox node pool (DaemonSet), so a
# user's first sandbox on a node doesn't pay the image pull (~31s measured).
# Image and pull policy are shared with the sandbox-pod PodTemplate; scheduling
# follows sandboxPod above. Only rendered when configMap.ENABLE_CRAFT is true.
#
# Costs ~3.3 GB of unreclaimable disk per sandbox node, on the same disk as the
# sandbox workspaces. Disable on nodes without that headroom. Pin global.version
# to an immutable tag when using this: a mutable tag like `latest` gets held at
# whichever digest the node pulled first.
sandboxImagePrepull:
  enabled: true
  resources:
    cpu: 10m
    memory: 32Mi
    ephemeralStorage: 64Mi
  # Name of an EXISTING PriorityClass to run the prepuller under. The chart
  # creates none, so an unset value means the cluster default priority. Point
  # this at a low/negative class if you want a pending sandbox pod to be able to
  # preempt the prepuller.
  priorityClassName: ""
  # Nodes that repull at once on a tag bump. Raise to warm a large pool faster,
  # at the cost of a thundering herd against the registry.
  updateMaxUnavailable: "25%"

# -- Sandbox egress proxy.
sandboxProxy:
  # Allow IPv6 (::/0 minus link-local/IMDS) egress from the proxy. OFF by default: the AWS
  # VPC CNI network-policy agent intermittently fails to program IPv6 ipBlocks into the
  # egress eBPF map (EINVAL), leaving it empty and fail-closed (proxy pods crash-loop), and
  # single-stack IPv4 clusters have no IPv6 egress to gate. Enable ONLY on dual-stack
  # clusters, where an Egress policy without an IPv6 allow silently drops all IPv6 egress.
  egressAllowIPv6: false
  replicaCount: 2
  terminationGracePeriodSeconds: 200
  progressDeadlineSeconds: 300
  confdirSizeLimit: 100Mi
  caSecretName: "sandbox-proxy-ca"
  caConfigMapName: "sandbox-proxy-ca-bundle"
  podAnnotations: {}
  serviceAccount:
    # Set IRSA annotations here when the sandbox proxy uses IAM-backed services,
    # such as RDS IAM auth.
    annotations: {}
    automount: true
  # Matches the `onyx` user (uid 1001) baked into the backend image.
  runAsUser: 1001
  fsGroup: 1001
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 2Gi
  nodeSelector: {}
  tolerations: []
  affinity: {}

# -- Additional arbitrary manifests to render as part of the release. Each entry
# may be either a YAML mapping (a single Kubernetes object) or a multi-line
# string that will be passed through `tpl` so it can reference release values.
# Useful for injecting resources not covered by the chart (e.g. NetworkPolicies,
# ExternalSecrets, custom CRs) without forking the chart.
extraManifests: []
# extraManifests:
#   - apiVersion: v1
#     kind: ConfigMap
#     metadata:
#       name: my-extra-config
#     data:
#       key: value
#   - |
#     apiVersion: networking.k8s.io/v1
#     kind: NetworkPolicy
#     metadata:
#       name: {{ include "onyx.fullname" . }}-extra
#     spec:
#       podSelector: {}
#       policyTypes:
#         - Ingress
