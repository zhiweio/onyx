# Zhihuiya pitfalls

- Do not turn on all ~35 servers for one pick.
- Patent detail backends overlap. Prefer `patent-briefing` + `patsnap-search`.
- Kind code is required on detail `pn`.
- News = `ls_web_search`, not `news_search`.
- `mcp-offload` dumps are not evidence. Use `replay_store.py`.
- Truncated seeds must be refetched.
- Landscape / monetize page caps are 20000. Search cap is 1000.
- Sequence identity is an integer percent from a tool.
